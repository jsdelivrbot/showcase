import { TypingCarouselDirective } from './typing-carousel.directive';

describe('TypingCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new TypingCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
